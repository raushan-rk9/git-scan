source 'https://rubygems.org'

git_source(:github) do |repo_name|
  repo_name = "#{repo_name}/#{repo_name}" unless repo_name.include?("/")
  "https://github.com/#{repo_name}.git"
end


# Bundle edge Rails instead: gem 'rails', github: '/rails'
gem 'rails', '~> 6.0'
# Use pg as the database for Active Record
gem 'pg', '~> 1.0'
# Use sqlite3 as the database for Active Record
# gem 'sqlite3'
# Use Puma as the app server
gem 'puma', '~> 3.7'
# Use SCSS for stylesheets
gem 'sassc-rails', '~> 1.3'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Dotenv to load environment variables
gem 'dotenv-rails', require: 'dotenv/rails-now'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.2'
# Turbolinks makes navigating your web application faster. Read more: https://github.com/turbolinks/turbolinks
gem 'turbolinks', '~> 5'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.5'
# Add support for authorization roles.
gem 'pundit'
# User authentication.
gem 'devise', :git => "https://github.com/heartcombo/devise.git", ref: '8bb358cf80a632d3232c3f548ce7b95fd94b6eb2'
# gem 'devise', '~> 4.4'
gem 'devise-security'
gem 'active_model_otp'

# Bootstrap framework.
gem 'bootstrap', '~> 4.0'
gem 'sprockets-rails', '>= 3.2.1'
gem 'jquery-rails', '~> 4.3'
gem 'jquery-datatables', '~> 1.10.20'
# Simpleform
gem 'simple_form'
# Font Awesome
gem 'font-awesome-sass', '~> 5.0'
# Material Design Icons
gem 'material_icons'
# File validators, for ActiveStorage validation
gem 'file_validators'
# Image processing, for image thumbnails
gem 'image_processing'
# Cocoon (for nested forms)
gem 'cocoon', '~> 1.2'
# Tablesorter (for javascript table sorting)
gem 'jquery-tablesorter'
# Use Redis adapter to run Action Cable in production
# gem 'redis', '~> 3.0'
# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'
# wysiwyg editor
gem 'summernote-rails', '~> 0.8.10'
# PDF generator
gem 'wicked_pdf'
gem 'wkhtmltopdf-binary', '~>0.12.5.4'

# *** GitHub ***
# JWT for GitHub access
gem 'jwt'

# OmniAuth for GitHub access
gem "omniauth-github"

# Octokit for GitHub access
gem "octokit", "~> 4.0"

# Spreadsheet for file access
gem 'spreadsheet'
gem 'roo'
# gem 'axlsx'

# sanitize to clean html
gem "sanitize", "~> 5.1.0"

# zip
gem "rubyzip", "~> 2.2"

# Client Side Validations
gem 'client_side_validations'
gem 'client_side_validations-simple_form'

# Cookie Notifications
gem 'cookies_eu'

# RTF
gem 'rrtf'
gem 'htmltoword'

gem 'repost'

gem 'i18n'

gem 'wdm', '>= 0.1.0' if Gem.win_platform?

group :development, :staging, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug', platforms: [:mri, :mingw, :x64_mingw]
  # Adds support for Capybara system testing and selenium driver
  gem 'capybara', '~> 3.9'
  gem 'selenium-webdriver'
  gem 'minitest-reporters'
end

group :development, :staging do
  # Access an IRB console on exception pages or by using <%= console %> anywhere in the code.
  gem 'web-console', '>= 3.3.0'
end

group :development, :staging, :test do
  gem 'listen', '>= 3.0.5', '< 3.2'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
  gem 'spring-watcher-listen', '~> 2.0.0'
  # Capistrano
  gem "capistrano", "~> 3.10", require: false
  gem "capistrano-rails", "~> 1.3", require: false
  gem 'capistrano-rbenv', '~> 2.1', require: false
  gem 'capistrano-bundler', require: false
  # ERD
  # gem "rails-erd"
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]

