<style>
table.dataTable thead .sorting_asc:before,
table.dataTable thead .sorting_asc:before,
table.dataTable thead .sorting_asc:before,
table.dataTable thead .sorting_asc:before,
table.dataTable thead .sorting_asc:before,
table.dataTable thead .sorting_asc:before {}

table th
{
  font-family: verdana !important;
  font-size:   14px    !important;
  line-height: 18px    !important;
}

table td
{
  font-family: verdana !important;
  font-size:   14px    !important;
  line-height: 18px    !important;
}
</style>
<h1>Code Checkmarks Hits <%= t('misc.list') %></h1>

<table id="scrollableDataTable" class="table table-striped table-bordered table-sm" cellspacing="0"  width="100%">
  <thead>
    <tr>
      <th data-data="checkmark_id">Checkmark ID</th>
      <th data-data="Hit At">Hit At</th>
      <th data-data="source_code">Source code ID</th>
      <th data-data="filename">Filename</th>
      <th data-data="line_number">Line number</th>
      <th data-data="code_statement">Statement</th>
    </tr>
  </thead>
  <tbody>
    <% @code_checkmark_hits.each do |code_checkmark_hit| %>
      <tr>
        <td>
          <%= link_to code_checkmark_hit.code_checkmark.checkmark_id, code_checkmark_code_checkmark_hits_path(code_checkmark_hit.code_checkmark) %>
        </td>
        <td>
          <%= code_checkmark_hit.hit_at.strftime('%Y-%m-%d %H:%M:%S.%N') %>
        </td>
        <td>
          <%= link_to code_checkmark_hit.code_checkmark.source_code.fullcodeid, item_source_code_path(code_checkmark_hit.code_checkmark.source_code.item_id, code_checkmark_hit.code_checkmark.source_code_id) %>
        </td>
        <td>
          <%= link_to File.basename(code_checkmark_hit.code_checkmark.filename), item_source_code_display_path(code_checkmark_hit.code_checkmark.source_code.item_id, code_checkmark_hit.code_checkmark.source_code_id) + '?new_window=true', target: :_blank %>
        </td>
        <td>
          <%= code_checkmark_hit.code_checkmark.line_number %>
        </td>
        <td>
          <%= code_checkmark_hit.code_checkmark.code_statement %>
        </td>
      </tr>
    <% end if @code_checkmark_hits.present? %>
    <% @code_conditional_blocks.each do |code_conditional_block| %>
      <tr>
        <td>
          N/A
        </td>
        <td>
          N/A
        </td>
        <td>
          Code Branch
        </td>
        <td>
          <%= File.basename(code_conditional_block.filename) %>
        </td>
        <td>
          <%= code_conditional_block.start_line_number %>
        </td>
        <td>
          <%= code_conditional_block.condition %>
        </td>
      </tr>
    <% end if @code_conditional_blocks.present? %>
  </tbody>
</table>

<br>

<%= link_to 'Back', go_back_path(1),  onclick: 'validateBack(1, this)', class: 'btn btn-default' %>

<% if @undo_path.present? && !session[:archives_visible].present? %>
  <%= link_to 'Undo', @undo_path, class: 'btn' %>
<% end %>

<% if @redo_path.present? && !session[:archives_visible].present? %>
  <%= link_to 'Redo', @redo_path, class: 'btn' %>
<% end %>

<script>
  if (document.cookie.match(/scrollTop=(\d+)/))
    $(window).scrollTop(document.cookie.match(/scrollTop=(\d+)/)[1]);

  $(document).ready
  (
    function()
    {
      var first     = true;
      var sortOrder = '[0, "asc"]';

      if (Cookies.get("checkmark_hits_table_order"))
        sortOrder = Cookies.get("checkmark_hits_table_order")
    
      if (document.cookie.match(/scrollTop=(\d+)/))
        $(window).scrollTop(document.cookie.match(/scrollTop=(\d+)/)[1]);

      $('#scrollableDataTable').DataTable
      (
        {
          "sDom":           "Rlfrtip",
          "scrollX":        true,
          "scrollY":        "55vh",
          "aLengthMenu":    [[10, 50, 100, -1], [25, 50, 100, "All"]],
          "pageLength":     -1,
          "scrollCollapse": true,
          "paging":         false,
          "ordering":       true,
          "info":           false,
          "drawCallback":   function(settings)
                            {
                              get_order(!first);
                            },
          "order":          JSON.parse(sortOrder)
        }
      );

      $(window).unload(function() {document.cookie = "scrollTop=" + $(window).scrollTop();});

      var tableOrder  = Cookies.get("checkmark_hits_table_order");

      function get_order(force)
      {
        if (!Cookies.get("checkmark_hits_table_order") || force)
        {
          var table   = $('#scrollableDataTable').DataTable();
          var date    = new Date();
          var minutes = 30;

          first       = false;

          date.setTime(date.getTime() + (minutes * 60 * 1000));
          Cookies.set('checkmark_hits_table_order', table.order(), { expires: date });

          return table.order();      
        }
        else
        {
          first       = false;

          return Cookies.get("checkmark_hits_table_order") ;
        }
      }
    }
  );
</script>