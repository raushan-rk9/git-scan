<ul class="list-group">
  <li class="list-group-item active">
    <h5>
      <%= "Id: #{@gitlab_access.id}" %>
    </h5>
  </li>
  <li class="list-group-item">
    <p>
      <strong>Username:</strong>
      <%= @gitlab_access.username %>
    </p>
    
    <p>
      <strong>Password:</strong>
      <%= @gitlab_access.display_password %>
    </p>
    
    <p>
      <strong>Token:</strong>
      <%= @gitlab_access.display_token %>
    </p>
    
    <p>
      <strong>URL:</strong>
      <%= @gitlab_access.url %>
    </p>
    
    <p>
      <strong>User:</strong>
      <%= @gitlab_access.user.email %>
    </p>
  </li>
</ul>

<%= link_to 'Edit', edit_gitlab_access_path(@gitlab_access), class: 'btn btn-default' %>
<%= link_to 'Back', go_back_path(1),  onclick: 'validateBack(1, this)', class: 'btn' %>

<% if @undo_path.present? && !session[:archives_visible].present? %>
  <%= link_to 'Undo', @undo_path, class: 'btn btn-default', title: @undo_message %>
<% end %>

<% if @redo_path.present? && !session[:archives_visible].present? %>
  <%= link_to 'Redo', @redo_path, class: 'btn btn-default', title: @redo_message %>
<% end %>

