<%= simple_form_for(import_path(@import), html: { class: 'form-horizontal', :method => "put", :id => "import-form" }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :project_select, 
                :collection => Project.all, 
                :label_method => :name,
                include_blank: true,
                :selected => @project.id,
                :input_html => { :id => "project_input" } %>
    <%= f.file_field :file %>
    <%= f.input :duplicates_permitted, as: :boolean, class: 'largerCheckbox'  %>

  <div class="form-actions">
    <%= f.button :submit, 'Load System Requirements', data: { "disable-with": t('misc.load') } %>
    <%= link_to 'Back', go_back_path(1),  onclick: 'validateBack(1, this)', class: 'btn btn-default' %>

    <% if @undo_path.present? && !session[:archives_visible].present? %>
      <%= link_to 'Undo', @undo_path, class: 'btn btn-default', title: @undo_message %>
    <% end %>
    <% if @redo_path.present? && !session[:archives_visible].present? %>
      <%= link_to 'Redo', @redo_path, class: 'btn btn-default', title: @redo_message %>
    <% end %>
  </div>
<% end %>