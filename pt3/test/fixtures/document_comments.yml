# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

doc_comment_001:
  commentid:      1
  comment:        "This document need to be updated. It's just the raw template."
  datemodified:   <%= Date.today %>
  status:         'Open'
  requestedby:    'test_3@airworthinesscert.com'
  assignedto:     'test_4@airworthinesscert.com'
  item_id:        <%= Item.find_by(identifier: 'HARDWARE_ITEM').try(:id) %>
  project_id:     <%= Project.find_by(identifier: 'TEST').try(:id) %>
  document_id:    <%= Document.find_by(docid: 'PHAC').try(:id) %>
  organization:   'test'
  draft_revision: '0.1'

doc_comment_002:
  commentid:      2
  comment:        "This document need to be updated. It's just the raw template."
  datemodified:   <%= Date.today %>
  status:         'Open'
  requestedby:    'test_3@airworthinesscert.com'
  assignedto:     'test_4@airworthinesscert.com'
  item_id:        <%= Item.find_by(identifier: 'SOFTWARE_ITEM').try(:id) %>
  project_id:     <%= Project.find_by(identifier: 'TEST').try(:id) %>
  document_id:    <%= Document.find_by(docid: 'PSAC').try(:id) %>
  organization:   'test'
  draft_revision: '0.1'